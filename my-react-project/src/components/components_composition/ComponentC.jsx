import React from "react";

const ComponentC  = ({emoji}) => { // Componente C que recibe props

    return (
        <div>{emoji}</div>
    )
}

export default ComponentC; // Exportamos el componente C